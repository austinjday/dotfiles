# encoding: utf-8
# module typing.re
# from C:\Users\austi\Anaconda3\lib\site-packages\astropy\table\_np_utils.cp36-win_amd64.pyd
# by generator 1.145
""" Wrapper namespace for re type aliases. """
# no imports

# functions

def Match(*args, **kwargs): # real signature unknown
    """
    Internal helper class for defining generic variants of concrete types.
    
        Note that this is not a type; let's call it a pseudo-type.  It cannot
        be used in instance and subclass checks in parameterized form, i.e.
        ``isinstance(42, Match[str])`` raises ``TypeError`` instead of returning
        ``False``.
    """
    pass

def Pattern(*args, **kwargs): # real signature unknown
    """
    Internal helper class for defining generic variants of concrete types.
    
        Note that this is not a type; let's call it a pseudo-type.  It cannot
        be used in instance and subclass checks in parameterized form, i.e.
        ``isinstance(42, Match[str])`` raises ``TypeError`` instead of returning
        ``False``.
    """
    pass

# no classes
# variables with complex values

__all__ = [
    'Pattern',
    'Match',
]

__weakref__ = None # (!) real value is ''

